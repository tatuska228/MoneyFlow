package com.example.moneyflow.ui.theme

import androidx.compose.ui.graphics.Color

// ── Brand / Primary ───────────────────────────────────────────────
val Primary         = Color(0xFF38A580)   // основной цвет приложения
val PrimaryDark     = Color(0xFF2D7A60)
val PrimaryLight    = Color(0xFF4DC99E)

// ── Backgrounds ───────────────────────────────────────────────────
val Background      = Color(0xFF38A580)   // фон главного экрана
val BackgroundDialog= Color(0xFF2D453D)   // фон диалогов и FAB
val BackgroundField = Color(0xFF3A5449)   // поля ввода внутри диалогов
val SurfaceVariant  = Color(0xFF236B52)

// ── Surfaces (auth screens) ───────────────────────────────────────
val Surface         = Color(0xFF38A580)

// ── Text ─────────────────────────────────────────────────────────
val OnPrimary       = Color(0xFFFFFFFF)
val OnBackground    = Color(0xFFFFFFFF)
val OnSurface       = Color(0xFFFFFFFF)
val TextSecondary   = Color(0xFFB8EDD6)
val TextHint        = Color(0xFF9ED4BC)

// ── Input fields (auth screens — white background) ────────────────
val InputBackground = Color(0xFFFFFFFF)
val InputText       = Color(0xFF1A1A1A)

// ── Buttons ───────────────────────────────────────────────────────
val ButtonPrimary   = Color(0xFF38A580)
val ButtonSecondary = Color(0xFF2D453D)   // тёмно-зелёный (совпадает с диалогами)
val ButtonDanger    = Color(0xFFE53935)
val ButtonCancel    = Color(0xFF3A5449)

// ── Mode toggle tabs ──────────────────────────────────────────────
val TabSelected     = Color(0xFF2D453D)
val TabUnselected   = Color(0xFF2A7A60)

val TextPrimary = Color(0xFF2A7A60)


// ── Divider / border ─────────────────────────────────────────────
val DividerColor    = Color(0xFF3A5449)
val Divider         = Color(0x33FFFFFF)

// ── Semantic ─────────────────────────────────────────────────────
val Income          = Color(0xFF4CAF50)
val Expense         = Color(0xFFF44336)
val Warning         = Color(0xFFFFC107)

// ── Category palette ─────────────────────────────────────────────
val CategoryBlue    = Color(0xFF2196F3)
val CategoryRed     = Color(0xFFE53935)
val CategoryOrange  = Color(0xFFFF9800)
val CategoryPurple  = Color(0xFF9C27B0)
val CategoryYellow  = Color(0xFFFFEB3B)
val CategoryTeal    = Color(0xFF009688)
val CategoryPink    = Color(0xFFE91E63)
val CategoryIndigo  = Color(0xFF3F51B5)
val CategoryLime    = Color(0xFF8BC34A)
val CategoryAmber   = Color(0xFFFFC107)